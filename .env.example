# n8n webhook URL — paste your webhook URL here for Phase 1
N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/retailer-brands

# Optional: proxy server for anti-bot handling (e.g. http://user:pass@host:port)
# PROXY_SERVER=http://user:pass@proxy:8080

# Optional: override browser user agent
# SCRAPER_USER_AGENT=Mozilla/5.0 (...)

# Optional: delay range in seconds (min,max) for request randomization
# SCRAPE_DELAY_MIN=1
# SCRAPE_DELAY_MAX=4
